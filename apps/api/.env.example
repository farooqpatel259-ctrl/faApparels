DATABASE_URL=postgresql://inventory:inventory@localhost:5432/inventory_ops
# On Render, use the Postgres connection string from the dashboard
# (usually includes ?sslmode=require).

JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
PORT=4000
NODE_ENV=development
